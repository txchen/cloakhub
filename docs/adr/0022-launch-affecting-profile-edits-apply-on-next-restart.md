# Launch-affecting profile edits apply on next restart

CloakHub will allow operators to save profile changes while a Browser Instance is running, but changes to launch-affecting settings such as proxy, fingerprint, display size, platform, GPU, user agent, and launch args apply only to the next Browser Instance start. CloakHub-side settings such as Sleep Policy can apply immediately.
