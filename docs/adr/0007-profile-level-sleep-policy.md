# Profile-level sleep policy

CloakHub will support a Sleep Policy on each Browser Profile, defaulting to the global 30 minute idle timeout but allowing a profile-specific timeout or an explicit never-sleep choice. Never-sleep profiles must be visually obvious in the UI because they directly trade resource efficiency for availability.
