# No startup auto-launch

CloakHub will not support CloakBrowser Manager's startup `auto_launch` behavior in v1. Browser Instances should start on demand through CDP or manual UI access, because launching browsers automatically on server startup works against CloakHub's resource-efficiency goal.
