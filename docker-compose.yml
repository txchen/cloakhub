services:
  cloakhub:
    build:
      context: .
    environment:
      CLOAKHUB_DATA_DIR: /data
      CLOAKHUB_HOST: 0.0.0.0
      CLOAKHUB_PORT: "7788"
    ports:
      - "127.0.0.1:7788:7788"
    volumes:
      - cloakhub-data:/data

volumes:
  cloakhub-data:
